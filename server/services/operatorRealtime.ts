export async function createOperatorRealtimeSession() {
  return { success: false, message: "Operator realtime not configured" };
}

export async function finalizeOperatorSession() {
  return { success: false, message: "Operator realtime not configured" };
}

export async function getOperatorSession() {
  return null;
}
